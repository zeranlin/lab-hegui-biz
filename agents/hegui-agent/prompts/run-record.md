# AI 调度运行记录要求

每次审查必须在本次审查产物输出目录下生成运行记录，文件名为 `xxx-AI调度运行记录.md`。

运行记录必须包含：

- 项目基本信息；
- 本次执行边界；
- AI 调度轮次；
- 每轮输入给 AI 的主要信息；
- 使用工具/命令；
- 抽取文本或临时辅助产物；
- 调用知识清单；
- 原文读取记录；
- 质量自检；
- 模型交互说明；
- 是否使用外部标注；
- 是否修改 LLM Wiki。

底层模型真实调用次数如果无法取得，应明确写：

```text
当前 Codex 环境不向仓库暴露底层模型实际调用次数，无法精确记录。
```
